# Example outputs

sinks:
  debug:
    type: console
    inputs:
      # - logs.apache
      # - logs.backup
      - logs.xp
      # - metrics.apache
      # - metrics.backup
      # - metrics.xp
      # - metrics.host
    target: stdout
    encoding: json
